<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h1>My movie lists</h1>
      <%@lists.each do |list|%>
        <h3><%=link_to list.name, list_path(list)%></h3>
      <%end%>
      <br>
      <%= link_to "Create a new list", new_list_path%>
    </div>
  </div>
</div>
